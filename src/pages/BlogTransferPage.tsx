import {useState}from"react";import type{StoreRecord}from"../lib/desktopApi";import{desktopApi}from"../lib/desktopApi";import{createAdminClient}from"../lib/shopifyClient";import{exportBlogs,importBlogs}from"../lib/admin-tools/blog-transfer";import{parseDataFile,type ExportFormat}from"../lib/admin-tools/adminTools";import{saveRows}from"../lib/admin-tools/saveRows";
export default function BlogTransferPage({activeStore}:{activeStore:StoreRecord|null}){const[file,setFile]=useState<File|null>(null),[format,setFormat]=useState<ExportFormat>("csv"),[mode,setMode]=useState<"create"|"update"|"upsert">("upsert"),[confirm,setConfirm]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState("");async function exp(){if(!activeStore)return;setBusy(true);setError("");try{const rows=await exportBlogs(createAdminClient(activeStore.id,activeStore.apiVersion),50000);const p=await saveRows(rows,format,`blogs-articles-${new Date().toISOString().slice(0,10)}`);setMessage(p?`Saved to ${p}`:"File save cancelled.");}catch(e){setError(String(e))}finally{setBusy(false)}}async function imp(){if(!activeStore||!file)return setError("Select a blog file.");if(confirm!=="YES")return setError("Type YES to confirm.");setBusy(true);try{const rows=await parseDataFile(file),r=await importBlogs(createAdminClient(activeStore.id,activeStore.apiVersion),rows,mode);setMessage(`Blogs created ${r.blogsCreated}, articles created ${r.articlesCreated}, updated ${r.articlesUpdated}, skipped ${r.skipped}, failed ${r.failed}${r.errors.length?`\n${r.errors.slice(0,15).join("\n")}`:""}`);await desktopApi.saveHistory({storeId:activeStore.id,kind:"blog-import",name:file.name,status:r.failed?"COMPLETED_WITH_ERRORS":"COMPLETED",total:r.total,processed:r.articlesCreated+r.articlesUpdated,createdCount:r.blogsCreated+r.articlesCreated,updatedCount:r.articlesUpdated,skippedCount:r.skipped,failedCount:r.failed,detailsJson:JSON.stringify(r)});}catch(e){setError(String(e))}finally{setBusy(false)}}return <div className="page-stack"><section className="page-hero"><div><span className="eyebrow">CONTENT · BLOGS</span><h2>Blog Import & Export</h2><p>Transfer blogs and articles with HTML, author, tags, images, and SEO.</p></div><aside><small>ACTIVE STORE</small><strong>{activeStore?.name||"No store"}</strong></aside></section>{error&&<div className="error-box">{error}</div>}{message&&<div className="success-box">{message}</div>}<section className="page-card"><header><div><h3>Export blogs</h3></div></header><div className="page-card-body"><div className="tool-form-grid"><div className="tool-field"><label>Format</label><select value={format} onChange={e=>setFormat(e.target.value as ExportFormat)}><option value="csv">CSV</option><option value="xlsx">XLSX</option><option value="json">JSON</option></select></div></div><div className="tool-actions"><button className="button-primary" disabled={busy||!activeStore} onClick={()=>void exp()}>Export blogs and articles</button></div></div></section><section className="page-card"><header><div><h3>Import blogs</h3></div></header><div className="page-card-body"><div className="tool-form-grid"><div className="tool-field wide"><label>CSV, XLSX, or JSON</label><input className="tool-file" type="file" accept=".csv,.xlsx,.xls,.json" onChange={e=>setFile(e.target.files?.[0]||null)}/></div><div className="tool-field"><label>Mode</label><select value={mode} onChange={e=>setMode(e.target.value as typeof mode)}><option value="upsert">Create or update</option><option value="create">Create only</option><option value="update">Update only</option></select></div><div className="tool-field"><label>Type YES to confirm</label><input value={confirm} onChange={e=>setConfirm(e.target.value)}/></div></div><div className="tool-actions"><button className="danger-button" disabled={busy||!activeStore} onClick={()=>void imp()}>Import blogs</button></div></div></section></div>}
